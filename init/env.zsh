# gemini
if [[ -f ~/.gemini ]]; then
    source ~/.gemini
fi
# proxy
if [[ -f ~/.gemini-http-proxy ]]; then
    source ~/.gemini-http-proxy
fi
# moltbot
if [[ -f ~/.moltbot ]]; then
    source ~/.moltbot
fi
