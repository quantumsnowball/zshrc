# gemini
if [[ -f ~/.gemini-api-key ]]; then
    source ~/.gemini-api-key
fi
# proxy
if [[ -f ~/.gemini-http-proxy ]]; then
    source ~/.gemini-http-proxy
fi
# moltbot
if [[ -f ~/.moltbot ]]; then
    source ~/.moltbot
fi
